# SGAR-MOT -- Python dependencies.
#
# These are the versions the released code was verified against (see
# docs/TUTORIAL.md §5). Install PyTorch first so that nothing downgrades it:
#
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch==2.4.1 torchvision==0.19.1
#   pip install -r requirements.txt
#
# ByteTrack (YOLOX) and SAM 2 are installed from their own repositories -- see
# docs/TUTORIAL.md -- and are not listed here. docker/Dockerfile does all of it.

numpy==1.23.5
opencv-python==4.10.0.84
Pillow==10.1.0
scipy==1.14.1
matplotlib==3.9.2
pandas==2.2.2
PyYAML==6.0.2

loguru==0.7.2
tqdm==4.66.5
tabulate==0.9.0
tensorboard==2.17.1

scikit-learn==1.6.0
scikit-image==0.24.0
torchmetrics==1.6.0

# tracking / evaluation
motmetrics==1.4.0
filterpy==1.4.5
lap==0.4.0
cython-bbox==0.1.5
h5py==3.11.0
thop==0.1.1.post2209072238
ninja==1.11.1.1

# SAM 2
hydra-core==1.3.2
iopath==0.1.10
